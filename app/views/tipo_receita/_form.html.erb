<%= form_with(model: tipo_receitum, local: true) do |form| %>
  <% if tipo_receitum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_receitum.errors.count, "error") %> prohibited this tipo_receita from being saved:</h2>

      <ul>
      <% tipo_receitum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao, id: :tipo_receita_descricao %>
  </div>

  <div class="actions">
    <%= form.submit 'Criar Receita'%>
  </div>
<% end %>
